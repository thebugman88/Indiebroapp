rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All suite records are accessed through authenticated, validated server operations.
    // Firebase Admin bypasses these rules; browser clients must never write scores,
    // subscription records, private conversations, or another artist's tracks.
    match /{document=**} { allow read, write: if false; }
  }
}
