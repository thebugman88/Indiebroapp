rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // All media is encrypted and accessed through authorized server routes.
    // Direct client writes would bypass quota and encryption enforcement.
    match /{allPaths=**} { allow read, write: if false; }
  }
}
